# NeuralOps — Python Dependencies

# Core ML
torch>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Kubernetes
kubernetes>=28.1.0

# API
fastapi>=0.110.0
uvicorn>=0.27.0
websockets>=12.0

# Observability clients
# Phase 2a: requests used by prometheus_fetcher.py to query Prometheus HTTP API
# Phase 2c: requests used by k8s_client/client.py to query Loki and Jaeger APIs
prometheus-client>=0.19.0
requests>=2.31.0

# Phase 2b: Trivy CLI must be installed separately (https://aquasecurity.github.io/trivy)
# kubernetes SDK already listed above — used by real_scanner.py + real_kubectl.py

# Logging
structlog>=24.1.0

# Config
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# LLM (shared with sentinel)
google-genai>=1.0.0
langchain>=0.1.0
langgraph>=0.0.40
